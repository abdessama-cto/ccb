---
name: go-cli-command-developer
description: When adding new commands, subcommands, or flags to the `ccb` CLI application, particularly within the `cmd/ccbootstrap` module, invoke this agent. It can assist in structuring commands using the `spf13/cobra` framework, ensuring proper argument parsing, flag definitions, consistent help messages, and adherence to Go best practices for CLI development.
tools:
  - read_file
  - write_file
  - shell_command
---
